import 'package:flutter/material.dart';

class SolicitudView extends StatelessWidget {
  SolicitudView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow,
      child: Center(child: Text('SOLICITUDES')),
    );
  }
}
